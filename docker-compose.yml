version: '3.8'

services:
  # AuraFlow Studio - Blazor Server UI integrated in API
  auraflow-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: auraflow-api
    ports:
      - "5000:5000"
    environment:
      - DOTNET_ENVIRONMENT=Development
      - ChatInterface__Enabled=true
      - ChatInterface__DefaultModel=Flux Dev
      - ChatInterface__MaxConcurrentGenerations=3
      - ChatInterface__TimeoutSeconds=120
      - ChatInterface__ApiBaseUrl=http://auraflow-api:5000
    volumes:
      - models:/app/models
      - output:/app/output
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  comfyui:
    image: ghcr.io/comfyanonymous/comfyui:latest
    container_name: auraflow-comfyui
    ports:
      - "8188:8188"
    volumes:
      - models:/comfyui/models
      - output:/comfyui/output
    environment:
      - COMFYUI_PORT=8188
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8188/system_stats"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  models:
  output:
